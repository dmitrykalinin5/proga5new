import Client.Client;
import Console.run;

public class Main {
    public static void main(String[] args) {
        Client client = new Client();
        // сделать session.run
        client.userInput();
    }
}